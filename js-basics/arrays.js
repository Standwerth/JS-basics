// Arrays 

let selectedColors = ['red', 'blue'];
selectedColors[2] = 'green';

// The the of elements are not restricted, in other words you can have differnt types of data in a array,
selectedColors[3] = 4;
console.log(selectedColors[0]);